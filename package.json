{
  "name": "impacts-sync-server",
  "version": "1.0.0",
  "description": "Node.js Express server for syncing data with Google BigQuery",
  "main": "sync-server.js",
  "scripts": {
    "start": "node sync-server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@google-cloud/bigquery": "^7.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}