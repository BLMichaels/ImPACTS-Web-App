{
  "buildCommand": "cd client && npm ci && CI=false npm run build",
  "ignoreCommand": "if [[ \"$VERCEL_GIT_COMMIT_REF\" == dependabot/* ]]; then echo \"Skipping Dependabot preview build\"; exit 0; else exit 1; fi",
  "outputDirectory": "client/build",
  "framework": "create-react-app",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
